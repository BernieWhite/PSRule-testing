
convention:
  include:
  - '.\GH-Hook'

input:
  pathIgnore:
  - 'out/'

configuration:
  # Use alternative environment tag
  CAF_EnvironmentTag: env
  # Use the following environments
  CAF_Environments: [ 'prod', 'dev', 'uat', 'test' ]
